{
  "name": "portfolio-jmfcool-org",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio site for Vercel deployment",
  "scripts": {
    "build": "mkdir -p . && echo build-complete",
    "start": "echo static-site"
  },
  "engines": {
    "node": ">=20"
  }
}
